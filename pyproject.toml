[tool.poetry]
name = "pyls_project"
version = "0.1.0"
description = "A simple directory listing tool"
authors = ["Sourav souravbiswas.000001@gmail.com"]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"